"""
collector パッケージ
---------------------------------------------------------
Bus-Vision公開HTMLからの時刻表収集(大阪シティバスからの正式な利用許可を
得られた後にのみ実行する)に関する準備コード一式。

詳細は collector/README.md を参照。現時点(許可待ち)では、この配下の
どのコードを呼び出してもネットワークアクセスは一切発生しない
(config.PERMISSION_GRANTED が False のため、http_client.py が
 ネットワークI/Oの直前で必ず例外を送出する)。

本番PWA(index.html / css / js/*.js / sw.js / data/*.json)とは完全に
独立しており、このパッケージを一切変更・実行しなくてもPWAの動作には
何の影響もない。
"""
