# 大阪シティバス 次のバス (MVP / デモ版)

「開いた瞬間、いつものバスがあと何分か分かる」をコンセプトにした、
毎日同じ大阪シティバスの停留所を使う人向けの、極めてシンプルなスマホ専用 PWA です。

高機能な乗換案内アプリではありません。地図もメニューも路線検索もありません。
アプリを開いたら、いつものバス停・いつもの方面の「次のバスまであと何分か」が
3秒以内にわかることだけを目的にしています。

## ⚠️ データについて(重要)

このアプリのデータは「停留所(名前・位置)」と「方面・時刻表」で扱いが異なります。

### 停留所名・緯度経度

`data/osaka-citybus-stops.json` にデータを配置すると、それが実データとして
使われます(現時点ではこのファイルは空 `[]` のため、後述のデモ停留所にフォールバックしています)。

- **合法的に利用可能な公開データのみを出典とすること**を前提に設計しています。
  優先出典は国土交通省「国土数値情報(バス停留所データ / P11)」です。
- 大阪シティバス公式サイトや「い・ま・ど・こ？」等のスクレイピングは一切行っていません。
- 非公開APIの解析・利用も行っていません。
- 詳細な形式・出典ルール・変換手順は [`data/README.md`](data/README.md) を参照してください。

### 方面・時刻表

**正式な方面・時刻表データはまだありません。**

- 停留所名・緯度経度が**実データ**の場合: 架空の発車時刻を本物のように表示しないため、
  方面プルダウンには選択肢を出さず(無効化)、次のバス表示欄には
  「🚧 時刻表データ準備中」と明示し、偽の時刻・カウントダウンは一切表示しません。
  この場合、画面右上の「DEMO」バッジも消えます(停留所位置は実データのため)。
- 停留所名・緯度経度が**デモ停留所**(実データ未投入時のフォールバック)の場合:
  従来どおり「DEMO」バッジを表示したまま、`js/data.js` 内の創作の方面・時刻表
  (なんば行 あと4分、等)を表示します。

大阪シティバスの GTFS-JP 等の正式データ利用許可が得られた際は、`js/data.js` の
`DemoBusDataSource` を正式データを返す実装に差し替えるだけで、
UI 側 (`js/app.js` / `index.html` / `css/style.css`) は変更せずに
そのまま利用できるように設計しています(データ層とUI層の分離)。

## 主な機能

1. Geolocation API による現在地取得
2. 現在地に近い順にバス停候補をプルダウン表示
3. バス停をプルダウンで変更可能
4. 行き先・方面もプルダウンで変更可能
5. 次のバスを大きく表示
6. 次の3便まで表示(1便目・2便目・3便目)
7. 各便に「あと○分」を表示
8. 最初の1便は特に大きく強調表示
9. 選択したバス停・方面を `localStorage` に保存
10. 次回起動時は前回利用したバス停・方面を自動表示
11. 「現在地から探す」ボタンでいつでも再検索可能
12. `manifest.json` + Service Worker による PWA 対応(ホーム画面追加・オフライン起動)
13. Android Chrome / iPhone Safari の両方を考慮した実装
14. スマホ最優先のレスポンシブ・ダークUI
15. HTTPS 環境での GPS 利用を前提とした設計(HTTPS 必須の Geolocation API)
16. 現在地取得後、近い順に最低10件(該当件数がそれ未満の場合は全件)をプルダウン表示
17. バス停プルダウンの各項目に現在地からの距離を表示(例: `大国町 420m`)。最寄り1件目は自動選択、残りは自由に選択可能
18. 停留所名・緯度経度をオープンデータ実データに差し替え可能な構造(`data/osaka-citybus-stops.json`)
19. 実データ停留所では、正式時刻表が無い間は架空の時刻を表示せず「時刻表データ準備中」と明示

## ファイル構成

```
osaka-nextbus/
├── index.html          画面本体(バス停・方面プルダウン、次のバス表示、現在地ボタン)
├── manifest.json        PWA マニフェスト
├── sw.js                Service Worker(静的アセットのキャッシュ)
├── css/
│   └── style.css        スマホ最優先のレスポンシブスタイル
├── js/
│   ├── data.js           データ層(BusDataSource)。実データ読み込み・デモ時刻表・距離計算ロジック
│   └── app.js             UI制御(プルダウン制御・表示更新・Geolocation・localStorage連携)
├── data/
│   ├── osaka-citybus-stops.json   実データ(停留所名・緯度経度)。現時点では空配列
│   └── README.md                   実データの形式・出典ルール・反映手順
├── scripts/
│   └── convert-ksj-p11.mjs         国土数値情報(P11)→ osaka-citybus-stops.json 変換スクリプト
├── icons/
│   ├── icon-192.png / icon-512.png   PWAアイコン(any / maskable 両対応)
│   ├── apple-touch-icon.png          iOS ホーム画面用アイコン
│   └── favicon-32.png                favicon
└── README.md
```

### データ層とUI層の分離について

`js/data.js` は `DemoBusDataSource` という1つのオブジェクトを通じてのみ
バス停・時刻表データを外部に公開しています(`init`, `getStops`,
`getStopsSortedByDistance`, `getNextDepartures` など)。`js/app.js` はこの
インターフェースのみに依存しており、データの中身(バス停名・座標・発車間隔)には
一切依存していません。

`DemoBusDataSource.init()` は起動時に一度だけ呼ばれ、`data/osaka-citybus-stops.json`
の読み込みを試みます。中身があればそれを実データとして採用し、空/読み込み失敗なら
自動的にデモ停留所へフォールバックします。将来 GTFS-JP 等の正式な時刻表データを
利用できるようになった場合は、同じインターフェースを実装したオブジェクトを作成し、
`js/app.js` 冒頭の `const dataSource = DemoBusDataSource;` を
差し替えるだけで移行できます。

### 実データ(停留所名・緯度経度)を反映する手順

このセッションの作業環境はネットワークポリシー上、GitHub関連ドメイン以外への
通信がブロックされており、国土数値情報のダウンロードサイト
(`nlftp.mlit.go.jp`)へ直接アクセスできませんでした。そのため、このリポジトリの
`data/osaka-citybus-stops.json` は現時点でも空 `[]` のままで、デモ停留所で
動作しています。実データを反映するには、お手数ですが以下の手順が必要です。

1. スマホのブラウザで [国土数値情報 バス停留所データ(P11)](https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-P11.html)
   を開き、大阪府のデータを **GeoJSON形式** でダウンロードする(利用規約への同意が必要な場合あり)。
2. GitHub の Web UI (`Add file → Upload files`) で、そのファイルをこのリポジトリの
   `data/raw/` 等にアップロードする。
3. Claude に「アップロードした実データを取り込んで」と伝える。
   `scripts/convert-ksj-p11.mjs` で大阪シティバス(大阪市高速電気軌道 / Osaka Metro Bus 等)
   の事業者に絞り込み、`data/osaka-citybus-stops.json` を生成・コミットする。

以降は同じ仕組み(`data/README.md` 参照)で誰でも差し替え・更新が可能です。

## 技術構成

- HTML / CSS / Vanilla JavaScript(フレームワーク・ビルドツール不使用)
- データは JSON 相当のプレーンオブジェクト(`js/data.js` 内)
- 状態保存は `localStorage`
- 現在地取得は `Geolocation API`
- PWA 対応(`manifest.json` + Service Worker によるオフラインキャッシュ)
- 外部通信・サーバーサイド処理なし。完全に静的ファイルのみで構成されており、
  GitHub Pages 等の静的ホスティングでそのまま公開可能です。

## ローカルでの動作確認方法

Geolocation API と Service Worker はセキュリティ上の理由から
`file://` では正しく動作しないため、簡易HTTPサーバー経由で確認してください
(`localhost` は例外的に HTTPS 扱いのため、ローカル確認時は HTTP で問題ありません)。

```bash
cd osaka-nextbus
python3 -m http.server 8000
# ブラウザで http://localhost:8000 を開く
```

スマホの実機で試す場合は GPS・Service Worker の制約上、
本番相当の HTTPS 環境(GitHub Pages 等)での確認を推奨します。

## 公開方法(例: GitHub Pages)

1. このリポジトリの `main` ブランチ(または任意の公開用ブランチ)に
   本アプリのファイル一式を配置する。
2. GitHub リポジトリの Settings → Pages で公開ブランチ・ルートディレクトリを指定する。
3. 発行された `https://<username>.github.io/<repo>/` にスマホでアクセスし、
   「ホーム画面に追加」から PWA としてインストールして動作確認する。

## 既知の制約(MVPの範囲)

- 停留所名・緯度経度は実データ化に対応済みですが、`data/osaka-citybus-stops.json`
  が空のうちはデモ停留所(6箇所)で動作します(上記「実データを反映する手順」参照)。
- 方面(行き先)・時刻表は実データの有無にかかわらず、現時点ではすべてデモです。
- 遅延・運休・臨時便などのリアルタイム情報には対応していません。
- 多言語対応(英語表記等)は未対応です。
